/**
 * @format
 */

import {AppRegistry} from 'react-native';

import {name as appName} from './app.json';
import HomePlays from './Apps/HomePlay/HomePlays';

AppRegistry.registerComponent(appName, () => HomePlays);
